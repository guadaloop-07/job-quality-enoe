# Copy this file to .env and replace the password before starting the database.
# .env is intentionally ignored and must never be committed.
POSTGRES_USER=enoe_owner
POSTGRES_PASSWORD=change-me-before-starting
POSTGRES_DB=enoe_local
# Optional: the Compose configuration defaults to 5435 when this is omitted.
POSTGRES_PORT=5435
